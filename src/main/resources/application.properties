# PostgreSQL Configuration

spring.datasource.url=jdbc:postgresql://localhost:5432/recipebook
spring.datasource.username=mhaslam #TODO ${PSQL_PASSWORD}
spring.datasource.password=somePWD
spring.jpa.hibernate.ddl-auto=update

# Hibernate Configuration
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Other Settings
server.port=8080
spring.jackson.serialization.indent_output=true


# Logging Settings
logging.level.org.springframework=INFO
logging.level.com.malcolmhaslam=DEBUG
logging.file.name=recipebook.log

#jwt config
jwt.secret="SOMEBASE64SECRET"
jwt.expiration=3600